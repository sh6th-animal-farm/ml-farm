<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.animalfarm.mlf.domain.project.FarmRepository">
	
	<select id="selectAllFarm" resultType="farmDTO">
		SELECT * from farms;
	</select>
	
	<insert id="insertFarm" parameterType="farmDTO">
        INSERT INTO farms (
            farm_name,
            address_sido,
            address_sigungu,
            address_street,
            address_details,
            latitude,
            longitude,   altitude,
            farm_type,
            area,
            description,
            open_at,
            thumbnail_url,
            created_at
        ) VALUES (
            #{farmName},
            #{addressSido},
            #{addressSigungu},
            #{addressStreet},
            #{addressDetails},
            #{latitude},
            #{longitude}, #{altitude},
            #{farmType},
            #{area},
            #{description},
            #{openAt},
            #{thumbnailUrl},
            NOW()
        )
    </insert>
	
</mapper>